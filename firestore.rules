rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper to check if user is admin
    function isAdmin() {
      return request.auth != null && 
        (request.auth.token.admin == true || request.auth.token.email == "matysallanet@gmail.com" || request.auth.uid == "admin");
    }

    // Helper to check if user owns or is legacy member of a document
    function isDocOwner(docData) {
      return request.auth != null && (
        docData == null || 
        docData.userId == request.auth.uid || 
        docData.creatorId == request.auth.uid || 
        docData.ownerId == request.auth.uid || 
        (docData.allowedUsers != null && request.auth.uid in docData.allowedUsers)
      );
    }

    // Accounts collection
    match /accounts/{accountId} {
      allow read, write: if request.auth != null && (isDocOwner(resource) || isAdmin());
    }

    // Debts collection
    match /debts/{debtId} {
      allow read, write: if request.auth != null && (isDocOwner(resource) || isAdmin());
    }

    // Wishlist collection
    match /wishlist/{wishId} {
      allow read, write: if request.auth != null && (isDocOwner(resource) || isAdmin());
    }

    // Transactions collection
    match /transactions/{transactionId} {
      allow read, write: if request.auth != null && (isDocOwner(resource) || isAdmin());
    }

    // Pockets collection
    match /pockets/{pocketId} {
      allow read, write: if request.auth != null && (isDocOwner(resource) || isAdmin());
    }

    // Friendships collection
    match /friendships/{friendshipId} {
      allow read, write: if request.auth != null && 
        (resource == null || resource.data.senderId == request.auth.uid || resource.data.receiverId == request.auth.uid || isAdmin());
    }

    // Users Metadata
    match /users_meta/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && (request.auth.uid == userId || isAdmin());
    }
  }
}
